#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	int T;
	// cin>>T;
	scanf("%d",&T);
	int n,x;
	while(T--)
	{
		// cin>>n;
		scanf("%d",&n);
		for(int i=1;i<=n;++i)scanf("%d",&x);
		for(int i=1;i<=n;++i)scanf("%d",&x);
		puts("Win!");
	}
}